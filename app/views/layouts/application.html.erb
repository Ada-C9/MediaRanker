<!DOCTYPE html>
<html lang="en">
  <head>
    <%= favicon_link_tag asset_path('owl.jpg'), :rel => 'icon', :type => 'image/jpg' %>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "Untitled" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "application", 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
  </head>

  <body>
      <header>
        <nav class="nav">
          <ul>
            <li>
              <h1>Media Ranker </h1>
            </li>
            <li>
              <h2>| Ranking the Best of Everything</h2>
            </li>
            <li>
              <%= link_to("View top media", works_path, class: "hollow button") %>
            </li>
            <li>
              <%= link_to("View all media", works_path, class: "hollow button") %>
            </li>
            <li>
              <%= link_to("Add a new work", new_work_path, class: "hollow button") %>
            </li>
            <li>
              <%= link_to("View all users", works_path, class: "hollow button") %>
            </li>
            <li>
              <%= link_to("Log in", class: "hollow button") %>
            </li>
          </ul>

        </nav>
      </header>

      <main>
        <%= yield %>
      </main>

      <footer>
      </footer>

    </body>
</html>
