<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />

  <title><%= content_for?(:title) ? yield(:title) : "Untitled" %></title>

  <%= stylesheet_link_tag    "application" %>
  <%= javascript_include_tag "application", 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>

<body>
  <header>
    <h2>
      <a href="/">Media Ranker</a>
      <small>Ranking the Best of Everything</small>
    </h2>
  <nav>
    <a class="button"><%= link_to "View top media", root_path%></a>
    <a class="button"><%= link_to "View all media", works_index_path%></a>
    <a class="button"><%= link_to "Add a new work", new_work_path %></a>
    <a class="button"><%= link_to "View all users", users_index_path%></a>
    <div>
      <a class="button" ><%= link_to "Login",login_path%></a>
      <a class="button" ><%= link_to "Logout", logout_path%></a>
    </div>

  </nav>
  </header>
  <ul>
    <% if @user %>
    <li><%= link_to "Login", login_path %></li>
    <% else %>
    <% if false %>
      <li><%= link_to "Logout", logout_path, method: :delete %></li>
      <% end %>

    <% end %>
  </ul>

  <%= yield %>

</body>
</html>
