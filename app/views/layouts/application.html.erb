<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "Media Ranker 1.0" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "application", 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
    <%= favicon_link_tag 'owl.jpg' %>
  </head>

  <body>
    <nav>
      <h1><%= link_to "Media Ranker", root_path %></h1>
      <h2>Ranking the Best of Everything</h2>

      <ul>
        <li><%= link_to "View top media", root_path %></li>
        <li><%= link_to "View all media", works_path %></li>
        <li><%= link_to "Add a new work", new_work_path %></li>
        <li><%= link_to "View all users", users_path %></li>
        <% if @user.nil? %>
          <li><%= link_to "Log In", login_form_path %></li>
        <% else %>
          <li><%= link_to "Log in as #{@user.name}", user_path(@user.id) %></li>
          <li><%= link_to "Log Out", logout_path, method: :delete %></li>
        <% end %>
      </ul>
    </nav>

    <main>
      <section class="flash">
        <% flash.each do |name, message| %>
            <strong><%= message %></strong>
        <% end %>
      </section>

      <%= yield %>

    </main>

  </body>
</html>
