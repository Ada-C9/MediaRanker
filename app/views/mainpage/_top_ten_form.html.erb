<% model.order_by_vote.take(10).each do |work|%>
<li>
  <h4>
  <%= link_to work.title, work_path(work)%>
  <small><%= work.created_by%></small>
  </h4>
  <p><%= work.total_votes%> votes</p>
</li>
<%end%>
