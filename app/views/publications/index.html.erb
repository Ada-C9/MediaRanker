<main class="row">
  <h3>List of works</h3>

  <section class="media-table">
      <table>
        <thead>
          <h4>Albums</h4>
          <tr>
            <th width=80>Votes</th>
            <th>Title</th>
            <th>Created By</th>
            <th width=100>Published</th>
            <th width=100>Upvote</th>
          </tr>
        </thead>
        <tbody>
          <% @albums.each do |album| %>
            <tr>
              <td><%= album.get_votes(album.id) %></td>
              <td><%= album.title %></td>
              <td><%=album.creator%></td>
              <td>2300</td>
              <td><%= link_to "Upvote", new_publication_vote_path(album.id), class: "button upvote" %> </td>
            </tr>
          <% end %>
        </tbody>
      </table>


      <table>
        <thead>
          <h4>Books</h4>
          <tr>
            <th width=80>Votes</th>
            <th>Title</th>
            <th>Created By</th>
            <th width=100>Published</th>
            <th width=100>Upvote</th>
          </tr>
        </thead>
        <tbody>
          <% @books.each do |book| %>
            <tr>
              <td><%= book.get_votes(book.id) %></td>
              <td><%= book.title %></td>
              <td><%= book.creator %></td>
              <td>2300</td>
                <td><%= link_to "Upvote", new_publication_vote_path(book.id), class: "button upvote" %> </td>
            </tr>
          <% end %>
        </tbody>
      </table>


      <table>
        <thead>
          <h4>Movies</h4>
          <tr>
            <th width=80>Votes</th>
            <th>Title</th>
            <th>Created By</th>
            <th width=100>Published</th>
            <th width=100>Upvote</th>
          </tr>
        </thead>
        <tbody>
          <% @movies.each do |movie| %>
            <tr>
              <td><%= movie.get_votes(movie.id) %></td>
              <td><%= movie.title %></td>
              <td><%= movie.creator %></td>
              <td>2300</td>
                <td><%= link_to "Upvote", new_publication_vote_path(movie.id), class: "button upvote" %> </td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </section>
</main>
