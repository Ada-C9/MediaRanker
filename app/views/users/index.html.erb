<h1>Usernames</h1>
<ul>
  <% @users.each do |user|  %>
    <li>
      <%= link_to user.username, author_path(author.id) %>  <%= link_to "Edit", edit_user_path(user.id) %>  <%= link_to "Delete", user_path(user.id), method: :delete %>
    </li>
  <% end %>
</ul>
