<main>
  <h2>List of Users</h2>
  <table>
    <thead>
      <tr>
        <th>Username</th>
        <th>Votes</th>
        <th>Joined</th>
      </tr>
    </thead>
    <tbody>
      <% @users.each do |user| %>
        <tr>
          <td><%= user.username %></td>
          <td><%= user.votes %></td>
          <td><%= display_date(user.created_at) %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</main>
