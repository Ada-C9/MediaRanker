<h3>List of Users</h3>

<table>
  <thead>
    <tr>
      <th>Username</th>
      <th>Votes</th>
      <th>Joined</th>
    </tr>
  </thead>
  <tbody>
    <% @users.each do |user| %>
    <tr>
      <td><%= user.username %></td>
      <td>votecount here</td>
      <td><%= user.created_at.strftime("%B %-d, %Y") %></td>
    </tr>
    <% end %>
  </tbody>
</table>

<%= link_to("Back to Media List")%>
