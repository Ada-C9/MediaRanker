<%= render partial: "layouts/error_messages", locals: { model: @user } %>

<h2>Log In</h2>

<%= form_for @user do |f| %>
  <%= f.label :username %>
  <%= f.text_field :name %>

  <%= f.submit %>
  <% end %>
