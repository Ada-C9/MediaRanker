
<article class="user-details">


<h3> User Summary: <%= @user.user_name%></h3>
<p>Joined site: <%= @user.created_at.to_date.to_formatted_s(:long_ordinal)%></p>
</article>

<article class="user-votes">
  <h4>Votes</h4>
  <table>
    <thead>
      <th>Media Title</th>
      <th>Created By</th>
      <th>Published</th>
      <th>Category</th>
      <th>Voted On</th>
    </thead>
    <tbody>
      <% @user.votes.each do |vote| %>
      <tr>

      <td>
        <%= link_to vote.work.title, work_path(vote.work_id) %>
      </td>
      <td>
        <%=vote.work.creator%>
      </td>
      <td>
        <%=vote.work.publication_year%>
      </td>
      <td>
        <%=vote.work.category%>
      </td>
      <td>
        <%=vote.created_at.to_date.to_formatted_s(:long_ordinal)%>
      </td>
      </tr>
      <%end%>
    </tbody>
  </table>

</article>
