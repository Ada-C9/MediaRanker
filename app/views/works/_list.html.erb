
  <table>
    <thead>
      <tr>
        <th width=80>Votes</th>
        <th>Title</th>
        <th>Created By</th>
        <th width=100>Published</th>
        <th width=100>Upvote</th>
      </tr>
    </thead>
    <tbody>
      <% @works.each do |work| %>
      <% if work.work_category == list_category %>
      <tr>
        <td> Num </td>
        <td> <%= link_to work.work_title, work_path(work.id) %> </td>
        <td> <%= work.work_creator %> </td>
        <td> <%= work.work_publication_year %> </td>
        <td><%= link_to 'Upvote', new_work_upvote_path(work.id), class: 'button' %></td>
      </tr>
      <% end%>
      <% end%>
    </tbody>
  </table>
