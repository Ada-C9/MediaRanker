<h4>List of Works</h4>

<h3>Albums</h3>
<table>
  <th>Votes</th>
  <th>Title</th>
  <th>Created By</th>
  <th>Published</th>
  <th>Upvote</th>

  <% @albums.each do |album| %>
  <tr>
  <td> <%= album.votes.count %> </td>
  <td><%= link_to(album.title, work_path(album))  %></td>
  <td><%= album.creator %></td>
  <td><%= album.publication_year %></td>
  <td><%= link_to "Upvote", upvote_path(album), class: "button" %></td>
  </tr>
  <% end %>
</table>

<h3>Books</h3>
<table>
  <th>Votes</th>
  <th>Title</th>
  <th>Created By</th>
  <th>Published</th>
  <th>Upvote</th>

  <% @books.each do |book| %>
  <tr>
  <td><%= book.votes.count %></td>
  <td><%= link_to(book.title, work_path(book)) %></td>
  <td><%= book.creator %></td>
  <td><%= book.publication_year %></td>
  <td><%= link_to "Upvote", upvote_path(book), class: "button" %></td>
  </tr>
  <% end %>
</table>

<h3>Movies</h3>
<table>
  <th>Votes</th>
  <th>Title</th>
  <th>Created By</th>
  <th>Published</th>
  <th>Upvote</th>

  <% @movies.each do |movie| %>
  <tr>
  <td><%= movie.votes.count %></td>
  <td><%= link_to(movie.title, work_path(movie))  %></td>
  <td><%= movie.creator %></td>
  <td><%= movie.publication_year %></td>
  <td><%= link_to "Upvote", upvote_path(movie), class: "button" %></td>
  </tr>
  <% end %>
</table>
