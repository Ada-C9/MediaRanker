
<h3>List of works</h3>

  <h4>Albums</h4>

  <%= render partial: "media_table", locals: {model: @works.where(category: "album")}%>


  <h4>Books</h4>

  <%= render partial: "media_table", locals: {model: @works.where(category: "book")}%>


  <h4>Movies</h4>

  <%= render partial: "media_table", locals: {model: @works.where(category: "movie")}%>

  <ul>
    <li><%= link_to "View top media", root_path, class: "button"%></li>
    <li><%= link_to "Add a new work", new_work_path, class: "button"%></li>
  </ul>
