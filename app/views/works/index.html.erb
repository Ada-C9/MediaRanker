<h1>List of works</h1>
<% @hash.each {|key, value| %>
  <h3><%= key %></h3>
  <% i = 0 %>
  <% while i < value.length %>
    <% work = value[i] %>
    <strong><%= work.title %></strong>
    <span> by <%= work.creator %></span>
    <br>
    <% i += 1%>
  <% end %>
<%  } %>
