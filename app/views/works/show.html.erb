<h3><%= @work.title%></h3>
<p>Created by: <%= @work.creator %></p>
<p>Published: <%= @work.publication_year %></p>
<p>Description: <%= @work.description %></p>

<%= link_to("Back to media ranks", root_path, class: "button") %>
<%= link_to("Edit", edit_work_path(@work.id), class: "button") %>
<%= link_to("Upvote", work_votes_path(@work.id), class: "button", method: :post) %>
<%= link_to("Delete", work_path(@work.id), class: "button", method: :delete) %>

<%# TODO: Fix time signature in work show table %>
<h3><%= @work.vote_count %> Votes for this <%= @work.category.capitalize %></h3>
<table>
  <thead>
    <tr>
      <th>User</th>
      <th>Date</th>
    </tr>
  </thead>
  <tbody>
    <% @work.votes.each do |vote| %>
    <tr>
      <td><%= link_to vote.user.name, user_path(vote.user.id) %></td>
      <td><%= vote.created_at %></td>
    </tr>
    <% end %>
  </tbody>
</table>
