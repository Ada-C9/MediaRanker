<h2><%= @work.title %></h2>
<p>
  <ul>
    <li>
      <%= @creation %>
    </li>
    <li>
      <%= @publish %>
    </li>
    <li>
      <%= @describe %>
    </li>
  </ul>
</p>

<p>
  <ul>
    <li><%= link_to "Back to media ranks", root_path %></li>
    <li><%= link_to "Edit", edit_work_path(@work.id), method: :update %></li>
    <li><%= link_to "Upvote",work_votes_path(@work.id), method: :post %></li>
    <li><%= link_to "Delete", work_path(@work.id),
    method: :delete %></li>
  </ul>
</p>

<h2><%= "#{@work.votes.count} Votes for this #{@work.category.capitalize}" %></h2>

<ul>
  <!-- how do I list all the users that voted for this? -->
  <!-- Go from works > votes > users -->
  <!-- Go directly for users -> Votes -->
  <!-- Go to votes -> if vote.work_id == work.id print users.find(vote.user_id) -->

  <li>username</li>
  <li>voted on</li>
</ul>
