<p>Media Spotlight:
</p>

<ul> Top Albums
  <% @top_albums.each do |album| %>
  <li>
    <strong><%= link_to(album.title, work_path(album)) %></strong>
    <%= link_to(album.created_by, work_path(album)) %>
  </li>
</ul>
<% end %>

<ul> Top Movies
  <% @top_movies.each do |movie| %>
  <li>
    <strong><%= link_to(movie.title, work_path(movie)) %></strong>
    <%= link_to(movie.created_by, work_path(movie)) %>
  </li>
</ul>
<% end %>

<ul> Top Books
  <% @top_books.each do |book| %>
  <li>
    <strong><%= link_to(book.title, work_path(book)) %></strong>
    <%= link_to(book.created_by, work_path(book)) %>
  </li>
</ul>
<% end %>
